import React from 'react'

const Row = ({ title }) => {
    return (
        <div className="row">
            <div className="row-title">
                {title}
            </div>
        </div>
    )
}

export default Row
